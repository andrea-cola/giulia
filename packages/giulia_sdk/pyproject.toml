[project]
name = "giulia-sdk"
version = "0.1.0"
description = "Layer interfaces and shared types for the giulia agent framework (planned)"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["giulia_sdk"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
