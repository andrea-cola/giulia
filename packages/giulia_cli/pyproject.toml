[project]
name = "giulia-cli"
version = "0.1.0"
description = "Command-line interface for the giulia agent framework (planned)"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["giulia_cli"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
